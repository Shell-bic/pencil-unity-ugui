interface:
  display_name: "Pencil System Architecture & uGUI"
  short_description: "Systematic UI-to-engineering conversion: reason about design structures first, then translate into Unity uGUI architectures."
  brand_color: "#2563EB"
  default_prompt: "Use $pencil-unity-ugui to perform systematic UI-to-engineering conversion. Analyze the global structure and semantic intent first, then generate the corresponding Unity uGUI plan or implementation."

policy:
  # Changed from true to explicit keywords to prevent accidental narrow track lock-in
  allow_implicit_invocation: false
